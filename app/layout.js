export const metadata = { title:"poetry-maker", description:"Interactive word ripple poetry creation tool for classroom use." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body>{children}</body></html>);
}
